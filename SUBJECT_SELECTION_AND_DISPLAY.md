# Добавление выбора предмета и отображения информации в расписании

## 🎯 **Новые функции:**

### 📚 **Выбор предмета для ученика:**
- ✅ При открытии расписания ученика показывается выбор предмета
- ✅ Список предметов загружается из базы данных
- ✅ Обязательный выбор предмета перед сохранением уроков
- ✅ Предмет сохраняется вместе с уроком

### 📊 **Отображение информации в слотах:**

#### **Для преподавателя:**
- ✅ **Желтые слоты** - рабочие часы (с текстом "Рабочий час")
- ✅ **Красные слоты** - занятые учениками (показывают предмет и имя ученика)
- ✅ **Зеленые слоты** - доступны для выбора
- ✅ **Синие слоты** - выбраны для добавления

#### **Для ученика:**
- ✅ **Зеленые слоты** - доступны для записи (с текстом "Доступно")
- ✅ **Фиолетовые слоты** - мои забронированные уроки (показывают предмет)
- ✅ **Серые слоты** - недоступны (преподаватель не работает)
- ✅ **Синие слоты** - выбраны для записи

## 🔧 **Технические изменения:**

### **HTML шаблон (`admin/lessons.html`):**
```html
<!-- Выбор предмета для ученика -->
<div id="subject-selection" class="subject-selection" style="display: none;">
    <div class="form-group">
        <label for="lesson-subject">Выберите предмет:</label>
        <select id="lesson-subject" class="form-control">
            <option value="">Выберите предмет...</option>
        </select>
    </div>
</div>
```

### **CSS стили (`admin-lessons.css`):**
```css
/* Выбор предмета */
.subject-selection {
    margin-bottom: 20px;
    padding: 15px;
    background: #f8f9fa;
    border-radius: 8px;
    border: 1px solid #e9ecef;
}

/* Информация в слотах */
.schedule-slot .slot-subject {
    font-weight: 600;
    color: #fff;
    text-shadow: 1px 1px 1px rgba(0,0,0,0.3);
}

.schedule-slot .slot-student {
    font-size: 9px;
    color: #fff;
    text-shadow: 1px 1px 1px rgba(0,0,0,0.3);
}
```

### **JavaScript функции (`admin-lessons.js`):**

#### **Загрузка предметов:**
```javascript
async function loadSubjectsForStudent() {
    const response = await fetch('/admin-lessons/api/subjects');
    const data = await response.json();
    
    const subjectSelect = document.getElementById('lesson-subject');
    subjectSelect.innerHTML = '<option value="">Выберите предмет...</option>';
    
    data.subjects.forEach(subject => {
        const option = document.createElement('option');
        option.value = subject.id;
        option.textContent = subject.name;
        subjectSelect.appendChild(option);
    });
}
```

#### **Отображение информации в слотах:**
```javascript
// Для преподавателя - занятые слоты
slot.innerHTML = `
    <div class="slot-subject">${lesson.subjectName}</div>
    <div class="slot-student">${lesson.studentName}</div>
`;

// Для ученика - мои уроки
slot.innerHTML = `
    <div class="slot-subject">${slotData.subjectName}</div>
    <div class="slot-student">Мой урок</div>
`;
```

### **API изменения:**

#### **Получение расписания преподавателя:**
```java
// Добавлена информация об уроках
List<Lesson> lessons = lessonRepository.findByTeacherAndLessonDateBetween(
    teacher,
    getDateFromDayAndHour("MONDAY", "12", weekOffset),
    getDateFromDayAndHour("SUNDAY", "23", weekOffset)
);

for (Lesson lesson : lessons) {
    Map<String, Object> lessonMap = new HashMap<>();
    lessonMap.put("dayOfWeek", lesson.getLessonDate().getDayOfWeek().name());
    lessonMap.put("startTime", lesson.getLessonDate().toLocalTime().toString());
    lessonMap.put("subjectName", lesson.getSubject().getName());
    lessonMap.put("studentName", lesson.getStudent().getName());
    lessonsData.add(lessonMap);
}
```

#### **Получение расписания ученика:**
```java
// Добавлена информация о забронированных уроках
List<Lesson> studentLessons = lessonRepository.findByStudentAndLessonDateBetween(
    student,
    getDateFromDayAndHour("MONDAY", "12", weekOffset),
    getDateFromDayAndHour("SUNDAY", "23", weekOffset)
);

for (Lesson lesson : studentLessons) {
    Map<String, Object> lessonData = new HashMap<>();
    lessonData.put("dayOfWeek", lesson.getLessonDate().getDayOfWeek().name());
    lessonData.put("startTime", lesson.getLessonDate().toLocalTime().toString());
    lessonData.put("subjectName", lesson.getSubject().getName());
    lessonData.put("teacherName", lesson.getTeacher().getName());
    bookedSlots.add(lessonData);
}
```

### **Новый метод в LessonRepository:**
```java
// Найти уроки студента в указанном временном интервале
@Query("SELECT l FROM Lesson l WHERE l.student = :student AND " +
       "l.lessonDate BETWEEN :startDate AND :endDate")
List<Lesson> findByStudentAndLessonDateBetween(@Param("student") User student, 
                                             @Param("startDate") LocalDateTime startDate, 
                                             @Param("endDate") LocalDateTime endDate);
```

## 🎨 **Визуальные улучшения:**

### **Слоты с информацией:**
- ✅ **Увеличенная высота** слотов (60px вместо 50px)
- ✅ **Двухстрочное отображение** (предмет + ученик/статус)
- ✅ **Тени текста** для лучшей читаемости
- ✅ **Адаптивный размер шрифта**

### **Выбор предмета:**
- ✅ **Стилизованный select** с рамкой и фоном
- ✅ **Понятная метка** "Выберите предмет"
- ✅ **Валидация** - предмет обязателен для сохранения

## 🔄 **Логика работы:**

### **Для ученика:**
1. Открываем расписание ученика
2. Показывается выбор предмета
3. Загружаются доступные слоты преподавателей
4. Загружаются забронированные уроки ученика
5. При выборе слотов и сохранении:
   - Проверяется выбор предмета
   - Создаются уроки с указанным предметом
   - Обновляется отображение

### **Для преподавателя:**
1. Открываем расписание преподавателя
2. Загружаются рабочие слоты (желтые)
3. Загружаются занятые слоты (красные) с информацией
4. При сохранении обновляется отображение

## 📊 **Примеры отображения:**

### **Преподаватель:**
```
[Рабочий час]  [Математика    ]  [Доступно]
               [Иван Петров   ]
```

### **Ученик:**
```
[Доступно]     [Математика    ]  [Недоступно]
               [Мой урок      ]
```

## 🚀 **Результат:**

- ✅ **Выбор предмета** - обязательный при создании уроков
- ✅ **Информативные слоты** - показывают предметы и учеников
- ✅ **Визуальная обратная связь** - понятно что происходит
- ✅ **Данные из БД** - актуальная информация об уроках
- ✅ **Улучшенный UX** - интуитивно понятный интерфейс

---

*Функционал выбора предмета и отображения информации реализован для системы Brainify* 