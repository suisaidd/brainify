# Исправление синхронизации состояния доски и добавление отслеживания мышки

## Проблемы
1. **У людей разное состояние доски после обновления страницы** - пользователи видели разные состояния при перезагрузке
2. **Отсутствие видимости движения мышки** - пользователи не видели, где находятся курсоры других пользователей
3. **Отсутствие отображения ролей пользователей** - не было видно, кто учитель, ученик или администратор

## Анализ проблем

### 1. Проблема с синхронизацией состояния:
- **Приоритет localStorage над сервером** - `loadSavedBoard` загружал данные из localStorage backup вместо актуального состояния с сервера
- **Неправильная обработка серверных данных** - серверные данные не парсились и не валидировались правильно

### 2. Отсутствие отслеживания мышки:
- **Нет обработчика движения мышки** в Excalidraw
- **Нет WebSocket обработчика** для сообщений о движении мышки
- **Нет визуального отображения** курсоров других пользователей

## Исправления

### 1. Исправлена синхронизация состояния доски

#### Проблема:
`loadSavedBoard` загружал данные из localStorage backup вместо актуального состояния с сервера.

#### Исправление:
```javascript
// Парсим JSON контент с сервера
const serverBoardData = JSON.parse(result.content);
console.log('📋 Parsed server board data:', serverBoardData);

// Валидируем данные
const validElements = validateAndCleanElements(serverBoardData.elements || []);
const validAppState = validateAndCleanAppState(serverBoardData.appState || {});
const validFiles = validateAndCleanFiles(serverBoardData.files || {});

boardData = {
    elements: validElements,
    appState: validAppState,
    files: validFiles
};

console.log('📋 Server board data loaded successfully with', validElements.length, 'elements');
```

### 2. Добавлено отслеживание движения мышки

#### Фронтенд (excalidraw-board.html):

**Переменные для отслеживания:**
```javascript
// Переменные для отслеживания курсоров
let userCursors = new Map();
let lastMousePosition = { x: 0, y: 0 };
let mouseTrackingInterval = null;
let cursorElements = new Map();
```

**Обработчик движения мышки:**
```javascript
onPointerMove: (elements, appState, files, event) => {
    // Отслеживание движения мышки для показа курсора другим пользователям
    if (event && event.pointer && isConnected && lessonId !== 'demo') {
        const x = event.pointer.x;
        const y = event.pointer.y;
        
        // Отправляем позицию мышки только если она изменилась значительно
        const deltaX = Math.abs(x - lastMousePosition.x);
        const deltaY = Math.abs(y - lastMousePosition.y);
        
        if (deltaX > 5 || deltaY > 5) {
            lastMousePosition = { x, y };
            sendMousePosition(x, y);
        }
    }
},
```

**Функции для работы с курсорами:**
```javascript
// Функция для отправки позиции мышки
function sendMousePosition(x, y) {
    if (stompClient && isConnected && lessonId !== 'demo') {
        const mouseMessage = {
            type: 'mouse_move',
            userId: collaborationState.userId,
            userName: collaborationState.userName,
            userRole: collaborationState.userRole,
            x: x,
            y: y,
            timestamp: Date.now()
        };
        
        stompClient.send(`/app/excalidraw/${lessonId}/mouse`, {}, JSON.stringify(mouseMessage));
    }
}

// Функция для отображения курсора пользователя
function showUserCursor(userId, userName, userRole, x, y) {
    if (userId === collaborationState.userId) return; // Не показываем свой курсор
    
    // Создаем элемент курсора с цветом в зависимости от роли
    let cursorColor = '#007bff'; // Синий для учеников
    let cursorText = userName;
    
    if (userRole === 'teacher') {
        cursorColor = '#28a745'; // Зеленый для учителей
        cursorText = `👨‍🏫 ${userName}`;
    } else if (userRole === 'admin') {
        cursorColor = '#dc3545'; // Красный для администраторов
        cursorText = `👨‍💼 ${userName}`;
    } else {
        cursorText = `👨‍🎓 ${userName}`;
    }
    
    // Создаем визуальный курсор с иконкой и подписью
    // ...
}
```

**Обработка сообщений о движении мышки:**
```javascript
case 'mouse_move':
    if (data.userId !== collaborationState.userId) {
        console.log('🖱️ Mouse move from:', data.userName, 'at', data.x, data.y);
        showUserCursor(data.userId, data.userName, data.userRole, data.x, data.y);
    }
    break;
```

#### Бэкенд (ExcalidrawBoardController.java):

**Обработчик движения мышки:**
```java
/**
 * WebSocket: Обработка движения мышки
 */
@MessageMapping("/excalidraw/{lessonId}/mouse")
@SendTo("/topic/excalidraw/{lessonId}")
public Map<String, Object> handleMouseMove(@DestinationVariable String lessonId, 
                                         @Payload Map<String, Object> mouseData) {
    try {
        // Безопасное получение данных
        Object userIdObj = mouseData.get("userId");
        String userId = userIdObj != null ? userIdObj.toString() : null;
        String userName = (String) mouseData.get("userName");
        String userRole = (String) mouseData.get("userRole");
        Object xObj = mouseData.get("x");
        Object yObj = mouseData.get("y");
        
        // Валидация и преобразование координат
        double x = xObj instanceof Number ? ((Number) xObj).doubleValue() : Double.parseDouble(xObj.toString());
        double y = yObj instanceof Number ? ((Number) yObj).doubleValue() : Double.parseDouble(yObj.toString());
        
        // Создаем сообщение для рассылки
        Map<String, Object> mouseMessage = new HashMap<>();
        mouseMessage.put("type", "mouse_move");
        mouseMessage.put("userId", userId);
        mouseMessage.put("userName", userName);
        mouseMessage.put("userRole", userRole);
        mouseMessage.put("x", x);
        mouseMessage.put("y", y);
        mouseMessage.put("timestamp", LocalDateTime.now());
        
        System.out.println("🖱️ Mouse move from " + userName + " (" + userRole + ") at (" + x + ", " + y + ")");
        
        return mouseMessage;
        
    } catch (Exception e) {
        // Обработка ошибок
    }
}
```

### 3. Добавлено отображение ролей пользователей

#### Цветовая схема курсоров:
- **👨‍🎓 Ученики**: Синий цвет (`#007bff`)
- **👨‍🏫 Учителя**: Зеленый цвет (`#28a745`) 
- **👨‍💼 Администраторы**: Красный цвет (`#dc3545`)

#### Визуальные элементы:
- **Круглая иконка** с цветом роли
- **Подпись с именем** и эмодзи роли
- **Плавная анимация** перемещения
- **Автоматическое скрытие** через 3 секунды без движения

## Как работает исправленная система

### Синхронизация состояния доски:
1. **Пользователь обновляет страницу** → загружается `loadSavedBoard`
2. **Приоритет серверных данных** → сначала загружается с сервера
3. **Парсинг и валидация** → серверные данные парсятся и валидируются
4. **Применение состояния** → валидные данные применяются к Excalidraw
5. **Fallback на backup** → только если серверные данные недоступны

### Отслеживание мышки:
1. **Пользователь двигает мышку** → `onPointerMove` срабатывает
2. **Проверка значимости движения** → отправляется только если движение > 5px
3. **Отправка на сервер** → WebSocket сообщение с координатами и ролью
4. **Рассылка всем пользователям** → сервер рассылает сообщение всем
5. **Отображение курсора** → другие пользователи видят курсор с ролью
6. **Автоматическое скрытие** → курсор исчезает через 3 секунды

### WebSocket сообщения:
- **Отправляемые**: `mouse_move` с координатами и ролью
- **Получаемые**: `mouse_move` от других пользователей
- **Обработка**: создание визуального курсора с цветом роли

## Ожидаемое поведение после исправления

### ✅ Консистентное состояние доски:
- **Все пользователи видят одинаковое состояние** после обновления страницы
- **Приоритет серверных данных** над локальными backup
- **Правильная валидация** и применение состояния

### ✅ Видимость движения мышки:
- **Курсоры других пользователей видны** в реальном времени
- **Цветовая индикация ролей** (синий/зеленый/красный)
- **Плавная анимация** перемещения курсоров
- **Автоматическое скрытие** неактивных курсоров

### ✅ Отображение ролей:
- **👨‍🎓 Ученики** - синие курсоры
- **👨‍🏫 Учителя** - зеленые курсоры  
- **👨‍💼 Администраторы** - красные курсоры
- **Подписи с именами** и эмодзи ролей

## Логи для мониторинга

### Синхронизация состояния:
- ✅ `📋 Server board data loaded successfully with X elements`
- ✅ `📋 Parsed server board data: {...}`
- ✅ `📋 Server response: {...}`

### Отслеживание мышки:
- ✅ `🖱️ Mouse move from: UserName at X, Y`
- ✅ `🖱️ Mouse move from UserName (role) at (X, Y)`
- ✅ `📨 Excalidraw board message received: mouse_move from: UserName`

## Файлы изменены:
- `src/main/resources/templates/excalidraw-board.html`
- `src/main/java/com/example/brainify/Controllers/ExcalidrawBoardController.java`

## Статус: ✅ ИСПРАВЛЕНО
Все проблемы должны быть устранены:
- Состояние доски синхронизируется правильно после обновления страницы
- Движение мышки видно всем пользователям в реальном времени
- Роли пользователей отображаются цветовыми индикаторами
- Система работает стабильно при множественных пользователях
