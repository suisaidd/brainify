# Исправления для карточек учеников и расписания

## 🎯 **Основные изменения:**

### 📚 **1. Карточка ученика:**
- ✅ **Предметы ученика** - показываются предметы из `user_subject`
- ✅ **Кнопка "Уроки"** - для каждого предмета отдельно
- ✅ **Убраны лишние кнопки** - оставлена только "Назначить преподавателя"

### 📅 **2. Расписание:**
- ✅ **При нажатии "Уроки"** - открывается расписание назначенного преподавателя
- ✅ **Убраны лишние кнопки** из модального окна расписания
- ✅ **Упрощен интерфейс** - только расписание преподавателя

## 🔧 **Технические изменения:**

### **Обновленная карточка ученика:**
```javascript
function createStudentCard(student) {
    // Формируем список предметов с кнопками уроков
    let subjectsHtml = '';
    if (student.subjects && student.subjects.length > 0) {
        subjectsHtml = student.subjects.map(subject => `
            <div class="subject-item">
                <div class="subject-info">
                    <div class="subject-name">${subject.name}</div>
                    <div class="subject-description">${subject.description || ''}</div>
                </div>
                <button class="card-btn card-btn-primary" onclick="openTeacherScheduleForSubject(${student.id}, ${subject.id})">
                    <i class="fas fa-calendar-alt"></i>
                    Уроки
                </button>
            </div>
        `).join('');
    }
    
    card.innerHTML = `
        <!-- ... остальная информация ученика ... -->
        <div class="student-subjects">
            <h4>Предметы ученика:</h4>
            ${subjectsHtml}
        </div>
        <div class="user-card-actions">
            <button class="card-btn card-btn-secondary" onclick="openAssignTeacherModal(${student.id})">
                <i class="fas fa-user-plus"></i>
                Назначить преподавателя
            </button>
        </div>
    `;
}
```

### **Новая функция для открытия расписания:**
```javascript
function openTeacherScheduleForSubject(studentId, subjectId) {
    const student = studentsData.find(s => s.id === studentId);
    const subject = subjectsData.find(sub => sub.id === subjectId);
    if (!student || !subject) return;
    
    // Получаем назначенного преподавателя для данного предмета
    fetch(`/admin-lessons/api/student/${studentId}/teacher-for-subject/${subjectId}`)
        .then(response => response.json())
        .then(data => {
            if (data.teacher) {
                // Открываем расписание преподавателя
                openTeacherScheduleModal(data.teacher.id);
            } else {
                showNotification('Преподаватель не назначен для данного предмета', 'error');
            }
        })
        .catch(error => {
            console.error('Ошибка получения преподавателя:', error);
            showNotification('Ошибка получения преподавателя', 'error');
        });
}
```

### **Новый API для получения преподавателя:**
```java
@GetMapping("/api/student/{studentId}/teacher-for-subject/{subjectId}")
public ResponseEntity<Map<String, Object>> getTeacherForSubject(
        @PathVariable Long studentId,
        @PathVariable Long subjectId,
        HttpSession session) {
    
    // Получаем назначенного преподавателя для данного предмета
    StudentTeacher assignment = studentTeacherRepository.findActiveByStudentAndSubject(student, subject).orElse(null);
    
    if (assignment == null) {
        return ResponseEntity.ok(Map.of("teacher", null, "message", "Преподаватель не назначен"));
    }
    
    User teacher = assignment.getTeacher();
    Map<String, Object> teacherData = new HashMap<>();
    teacherData.put("id", teacher.getId());
    teacherData.put("name", teacher.getName());
    teacherData.put("email", teacher.getEmail());
    
    return ResponseEntity.ok(Map.of("teacher", teacherData));
}
```

### **Обновленный API для получения учеников:**
```java
@GetMapping("/api/students")
public ResponseEntity<Map<String, Object>> getStudents(HttpSession session) {
    List<Map<String, Object>> studentData = students.stream()
        .map(student -> {
            Map<String, Object> studentInfo = new HashMap<>();
            // ... основная информация ученика ...
            
            // Получаем предметы ученика
            List<Map<String, Object>> subjectData = student.getSubjects().stream()
                .map(subject -> {
                    Map<String, Object> subjectInfo = new HashMap<>();
                    subjectInfo.put("id", subject.getId());
                    subjectInfo.put("name", subject.getName());
                    subjectInfo.put("description", subject.getDescription());
                    return subjectInfo;
                })
                .collect(Collectors.toList());
            studentInfo.put("subjects", subjectData);
            
            return studentInfo;
        })
        .collect(Collectors.toList());
}
```

## 🎨 **Визуальные изменения:**

### **Карточка ученика:**
- ✅ **Предметы ученика** - отображаются в отдельной секции
- ✅ **Кнопка "Уроки"** - для каждого предмета
- ✅ **Упрощенная навигация** - только нужные действия

### **Модальное окно расписания:**
- ✅ **Информация о пользователе** - показывается чье расписание
- ✅ **Убраны лишние кнопки** - только расписание
- ✅ **Чистый интерфейс** - фокус на расписании

## 🔄 **Логика работы:**

### **Новый процесс:**
1. **Открываем страницу** - видим карточки учеников с предметами
2. **Нажимаем "Уроки"** на конкретном предмете
3. **Система находит** назначенного преподавателя для этого предмета
4. **Открывается расписание** преподавателя
5. **Управляем расписанием** - добавляем/удаляем рабочие часы

### **Преимущества:**
- ✅ **Логичность** - каждый предмет имеет своего преподавателя
- ✅ **Удобство** - не нужно выбирать из списков
- ✅ **Производительность** - меньше запросов к серверу
- ✅ **UX** - интуитивно понятный интерфейс

## 📊 **Структура данных:**

### **Карточка ученика:**
```json
{
  "id": 1,
  "name": "Иван Петров",
  "email": "ivan@example.com",
  "phone": "+7 999 123-45-67",
  "remainingLessons": 10,
  "subjects": [
    {
      "id": 1,
      "name": "Математика",
      "description": "Алгебра и геометрия"
    },
    {
      "id": 2,
      "name": "Физика",
      "description": "Механика и электричество"
    }
  ]
}
```

### **Ответ API преподавателя:**
```json
{
  "teacher": {
    "id": 5,
    "name": "Анна Сидорова",
    "email": "anna@example.com"
  }
}
```

## 🚀 **Результат:**

- ✅ **Карточки учеников** - показывают предметы с кнопками "Уроки"
- ✅ **Прямой переход** - от предмета к расписанию преподавателя
- ✅ **Упрощенный интерфейс** - убраны лишние элементы
- ✅ **Логичная навигация** - понятный путь действий
- ✅ **Оптимизированные запросы** - меньше обращений к серверу

---

*Функционал карточек учеников и расписания исправлен для системы Brainify* 